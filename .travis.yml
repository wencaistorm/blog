language: node_js
node_js: stable
branches:
  only:
  - hexo
cache:
  directories:
    - node_modules
before_install:
- npm install -g hexo
- npm install -g hexo-cli
before_script:
    - git config --global user.name '18037143856'
    - git config --global user.email 'zhang.wc@neusoft.com'

install:
- npm install
script:
- hexo clean 
- hexo g

after_script:
  - cd ./public
  - git init
  - git config --global user.name 'wencaistorm'
  - git config --global user.email '1052642137@qq.com'
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master

env:  
 global:   
   - GH_REF: github.com/wencaistorm/wencaistorm.github.io.git
